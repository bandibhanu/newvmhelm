{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "newvmhelm-chart.serviceAccountName" . }}
  labels:
{{ include "newvmhelm-chart.labels" . | indent 4 }}
{{- end -}}
