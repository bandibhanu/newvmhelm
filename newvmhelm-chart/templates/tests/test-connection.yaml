apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "newvmhelm-chart.fullname" . }}-test-connection"
  labels:
{{ include "newvmhelm-chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "newvmhelm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
